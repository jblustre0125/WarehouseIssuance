# Includes

Shared PHP includes live here.

- `auth.php` - session, roles, app URL helpers, and legacy navbar helper.
- `db_connect.php` - SQL Server connection and query helpers.
- `config.php` - local database/printer settings.
- `zebra_print.php` - Zebra label printing helper.
